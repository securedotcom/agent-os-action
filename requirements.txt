# Core AI providers
anthropic>=0.40.0
openai>=1.56.0
tenacity>=9.0.0

# AWS SDK for Foundation-Sec-8B
boto3>=1.40.68
botocore>=1.40.68

# Foundation-Sec-8B provider (optional, for local inference)
# Install only if using Foundation-Sec provider:
# pip install transformers torch accelerate huggingface-hub
# transformers>=4.48.0
# torch>=2.6.0
# accelerate>=1.2.0
# huggingface-hub>=1.1.2

# Security scanning tools
semgrep>=1.100.0  # SAST scanning with 2,000+ security rules

# Threat modeling
pytm>=1.3.0  # Deterministic threat modeling (STRIDE analysis)

# Additional security dependencies
pyyaml>=6.0.2  # CVE-2020-14343 fixed in 5.4+
requests>=2.32.0  # Multiple CVEs fixed
# urllib3 version managed by botocore to avoid conflicts
certifi>=2024.8.30  # Certificate validation
cryptography>=44.0.0  # Multiple CVEs fixed
